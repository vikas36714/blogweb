export interface IBlog {
        id: number;
        title: string;
        category: string;
        is_featured: boolean;
        is_active: boolean;
        image: string;
        description: string;
}
